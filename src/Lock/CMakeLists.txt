SET (LOCKDIR "../Lock")

set(Lock_SRCS
	${LOCKDIR}/Comparator.cpp
	${LOCKDIR}/Comparator.h
	${LOCKDIR}/Lock.cpp
	${LOCKDIR}/Lock.h
	${LOCKDIR}/SingleLock.cpp
	${LOCKDIR}/SingleLock.h CACHE INTERNAL "" ) 

